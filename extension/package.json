{
  "name": "adbrain-feed-manager",
  "version": "1.0.0",
  "description": "AdBrain Feed Manager Extension",
  "scripts": {
    "start": "http-server . -p 8080 -c-1 --cors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
} 